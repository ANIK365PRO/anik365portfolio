import { ProjectsSection } from '@/components/ProjectsSection';

const ProjectPage = () => {
    return (
        <div>
           <ProjectsSection/>
        </div>
    );
};

export default ProjectPage;